import 'package:flutter/material.dart';

class StepTest extends StatelessWidget {
  const StepTest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "Step Test Screen",
          style: TextStyle(fontSize: 30),
        ),
      ),
    );
  }
}